import React, { Component } from 'react'
import {View, TextInput, TouchableOpacity, Image, StyleSheet, Text} from 'react-native'
import theme from '../config/theme'

export default class CropImagePage extends Component {
  render () {
    return <View style={{width: theme.screenWidth, height: theme.screenWidth, backgroundColor: 'yellow'}}></View>
  }
}
